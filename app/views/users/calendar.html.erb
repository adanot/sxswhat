<div style="text-align:center; margin: 10px auto">
<div id='list' class='btn btn-success center'>Schedule</div>
<div id='cal' class='btn btn-primary center'>Calendar</div>
</div>
<script language="javascript" type="text/javascript">

  jQuery(
    function calview($) {
      $('#cal').on('click', function() {
        $("#calview").html('<%= escape_javascript(render "calview").html_safe %>');
        $("#listview").empty();
      });
    }
  );

  jQuery(
    function listview($) {
      $('#list').on('click', function() {
        $("#listview").html('<%= escape_javascript(render "listview").html_safe %>');
        $("#calview").empty();
      });
    }
  );
</script>

<div id="calview"></div>
<div id="listview">
  <%= render "listview" %>
</div>
